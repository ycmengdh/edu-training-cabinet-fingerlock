; PlatformIO 配置 - ESP32 指纹锁下位机（V2.0 Mesh版本）
; 仅用于编译验证，未配置上传/调试
[platformio]
src_dir = .
default_envs = esp32dev

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
build_flags =
    ; 启用 ESP-MESH 支持
    -DCORE_DEBUG_LEVEL=0
    ; ArduinoJson v6 兼容
    -DARDUINOJSON_ENABLE_ARDUINO_STRING=1
    -DARDUINOJSON_ENABLE_STD_STRING=1
    ; 启用 FatFS 长文件名支持（SD 卡中文文件名/路径）
    -DCONFIG_FATFS_LFN_HEAP=1
lib_deps =
    adafruit/Adafruit Fingerprint Sensor Library @ ^2.1.3
    bblanchon/ArduinoJson @ ^6.21.5
    ; SD 卡支持（Arduino-ESP32 内置 SD 库 + FatFS）
    SD
    FS
    SPI
