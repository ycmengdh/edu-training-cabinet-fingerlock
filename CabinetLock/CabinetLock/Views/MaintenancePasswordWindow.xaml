<Window x:Class="CabinetLock.MaintenancePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="维护密码" Width="430" Height="370" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="{StaticResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="柜子维护模式密码" Style="{StaticResource TitleText}"/>
        <TextBlock x:Name="VersionText" Grid.Row="1" Margin="0,5,0,18"
                   Style="{StaticResource PageStatusText}"/>
        <TextBlock Grid.Row="2" Text="新密码（6 位，仅可使用 1-3）" Style="{StaticResource LabelText}"/>
        <PasswordBox x:Name="NewPinBox" Grid.Row="3" Height="38" MaxLength="6" Margin="0,6,0,12"/>
        <StackPanel Grid.Row="4">
            <TextBlock Text="确认新密码" Style="{StaticResource LabelText}"/>
            <PasswordBox x:Name="ConfirmPinBox" Height="38" MaxLength="6" Margin="0,6,0,0"/>
            <TextBlock x:Name="StatusText" Margin="0,9,0,0" Foreground="{StaticResource DangerBrush}"/>
        </StackPanel>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Style="{StaticResource SecondaryButton}" Width="86"
                    Click="CancelButton_Click"/>
            <Button x:Name="SaveButton" Content="保存并同步" Width="112" Margin="8,0,0,0"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
