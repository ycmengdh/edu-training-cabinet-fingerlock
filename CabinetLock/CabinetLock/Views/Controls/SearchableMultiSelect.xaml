<UserControl x:Class="CabinetLock.Controls.SearchableMultiSelect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root" Height="38">
    <Grid>
        <ToggleButton x:Name="DropDownButton" Focusable="False">
            <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="SelectorBorder" Background="{StaticResource InputBrush}"
                            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="7">
                        <Grid Margin="11,0,10,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="SummaryText" Text="{Binding SelectionSummary, ElementName=Root}"
                                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                                       Foreground="{StaticResource TextBrush}"/>
                            <Path Grid.Column="1" Width="8" Height="5" Margin="12,0,0,0"
                                  Stretch="Fill" Fill="{StaticResource SubTextBrush}"
                                  Data="M 0 0 L 4 4 L 8 0 Z" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="SelectorBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="SelectorBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            <Setter TargetName="SelectorBorder" Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ToggleButton.Template>
        </ToggleButton>

        <Popup x:Name="DropDownPopup" PlacementTarget="{Binding ElementName=DropDownButton}"
               Placement="Bottom" VerticalOffset="4" AllowsTransparency="True"
               PopupAnimation="Fade" StaysOpen="False"
               IsOpen="{Binding IsChecked, ElementName=DropDownButton, Mode=TwoWay}"
               Opened="DropDownPopup_Opened">
            <Border Width="{Binding ActualWidth, ElementName=Root}" MinWidth="420" MaxHeight="340"
                    Background="{StaticResource CardBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="7" Padding="10"
                    Effect="{StaticResource CardShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="&#xE721;" FontFamily="{StaticResource AppIconFont}"
                                   Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center"
                                   Margin="3,0,8,0"/>
                        <TextBox x:Name="SearchBox" Grid.Column="1" Height="34"
                                 ToolTip="输入名称或编号搜索" TextChanged="SearchBox_TextChanged"/>
                    </Grid>
                    <Grid Grid.Row="1" Margin="0,8,0,6">
                        <TextBlock Text="{Binding FilterSummary, ElementName=Root}"
                                   Foreground="{StaticResource SubTextBrush}" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="全选可用项" Style="{StaticResource CompactSecondaryButton}"
                                    Height="28" Padding="10,0" Click="SelectAvailableButton_Click"/>
                            <Button Content="清空" Style="{StaticResource CompactSecondaryButton}"
                                    Height="28" Padding="10,0" Margin="6,0,0,0" Click="ClearButton_Click"/>
                        </StackPanel>
                    </Grid>
                    <ListBox x:Name="OptionList" Grid.Row="2" MaxHeight="230"
                             BorderThickness="0" Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="5,7">
                                    <Grid Width="360">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1.4*"/>
                                            <ColumnDefinition Width="1.1*"/>
                                            <ColumnDefinition Width="60"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding PrimaryText}" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="1" Text="{Binding SecondaryText}"
                                                   Foreground="{StaticResource SubTextBrush}" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="2" Text="{Binding StatusText}"
                                                   HorizontalAlignment="Right" Foreground="{StaticResource SubTextBrush}"/>
                                    </Grid>
                                </CheckBox>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Popup>
    </Grid>
</UserControl>
