<local:BorderlessWindow x:Class="FingerprintLockManager.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FingerprintLockManager"
        Title="关于"
        Width="460" MinHeight="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Window.Resources>
        <Style x:Key="AboutContextMenu" TargetType="ContextMenu">
            <Setter Property="MinWidth" Value="136"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="{StaticResource AppFont}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7" Padding="{TemplateBinding Padding}">
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AboutMenuItem" TargetType="MenuItem">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="ItemSurface" Background="{TemplateBinding Background}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemSurface" Property="Background" Value="{StaticResource SurfaceHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="28,20,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Logo + 名称 + 版本 -->
        <StackPanel HorizontalAlignment="Center" Margin="0,8,0,0">
            <Border Width="80" Height="80" CornerRadius="16"
                    Background="{StaticResource BackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    HorizontalAlignment="Center">
                <Image Source="pack://application:,,,/Resources/logo.ico"
                       Width="52" Height="52"
                       Stretch="Uniform"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <TextBlock x:Name="AppNameText"
                       Text="实训柜指纹权限管理系统"
                       Style="{StaticResource TitleText}"
                       FontSize="18"
                       HorizontalAlignment="Center" TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="12,16,12,0"/>
            <TextBlock x:Name="VersionText"
                       Text="版本 1.0.0"
                       Foreground="{StaticResource SubTextBrush}" FontSize="13"
                       HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>

        <!-- 联系信息 -->
        <Border Grid.Row="1" Style="{StaticResource Panel}" Margin="0,22,0,20" Padding="18,16">
            <StackPanel>
                <TextBlock Text="作者联系" Style="{StaticResource LabelText}" Margin="0,0,0,10"/>
                <TextBlock x:Name="AuthorEmailText"
                           Text="yc_mdh@qq.com"
                           FontSize="15" FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryBrush}"
                           Cursor="Hand"
                           ToolTip="左键：打开邮件&#x0a;右键：复制邮箱"
                           MouseLeftButtonUp="AuthorEmailText_MouseLeftButtonUp">
                    <TextBlock.ContextMenu>
                        <ContextMenu Style="{StaticResource AboutContextMenu}">
                            <MenuItem Header="复制邮箱" Style="{StaticResource AboutMenuItem}"
                                      Click="CopyEmailMenuItem_Click"/>
                            <MenuItem Header="发送邮件" Style="{StaticResource AboutMenuItem}"
                                      Click="SendEmailMenuItem_Click"/>
                        </ContextMenu>
                    </TextBlock.ContextMenu>
                </TextBlock>
                <TextBlock Text="左键打开发送邮件 · 右键可复制邮箱"
                           Foreground="{StaticResource SubTextBrush}" FontSize="11"
                           Margin="0,10,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Button Grid.Row="2" Content="关闭" Width="100" Height="36"
                HorizontalAlignment="Right" IsDefault="True" IsCancel="True"
                Click="CloseButton_Click"/>
    </Grid>
</local:BorderlessWindow>
